interface:
  display_name: "Zotero Research Assistant"
  short_description: "Search and analyze a local Zotero library without MCP"
  default_prompt: "Use $zotero-research-assistant to inspect my Zotero library and ground the answer in actual library data."

policy:
  allow_implicit_invocation: true
